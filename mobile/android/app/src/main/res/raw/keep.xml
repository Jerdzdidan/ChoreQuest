<?xml version="1.0" encoding="utf-8"?>
<!-- The notification icon is looked up by name at run time, which the release
     build's resource shrinker cannot see. Without this it would be stripped
     and every notification would fail to show. -->
<resources xmlns:tools="http://schemas.android.com/tools"
    tools:keep="@drawable/ic_notification" />
